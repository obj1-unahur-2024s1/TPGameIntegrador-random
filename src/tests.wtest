import wollok.game.*
import weapons.*
import juego.*
import ships.*
import sonidos.*
import tablero.*
import vidas.*


	
	
	
	test "funciona recibir impacto"{
		vidas.perderVida()
		assert.that(vidas.vidas() == 1)
	
	}
	test "funciona el cambiar de imagen"{
		vidasMotherShip.perderVida()
		assert.that(vidasMotherShip.image()=="image/barra3-4.jpeg")
	}
	
	test "al ganar el unico visual es el de youWin"{
		juego.youWin()
		assert.that(game.allVisuals().size()==1)
	}
	test "ver que funciona la funcion de jefe final"{
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		puntos.sumarPunto()
		assert.that(game.allVisuals().size() == 4)//motherShip, mainShip, y sus respectivas vidas
	}
